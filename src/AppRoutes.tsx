import { Dashboard } from '../src/components';

const approutes = [
    {
        path: '/dashboard',
        component: Dashboard, // Dynamically change the Dashboard component here
        // Add more routes here
    }
];
export default approutes;